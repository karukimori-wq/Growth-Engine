# Automated verification

The repository CI is the default automated gate for Professional integration changes. It should catch type, contract, Next build, and OpenNext build failures before a release owner is asked to touch Cloudflare Production.
