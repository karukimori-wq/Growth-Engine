# MVP integration identity

Professional integration requests preserve the MVP identity model:

- `workspaceId`
- `userId`

`professionalId` is not introduced as a mandatory MVP integration key. Additional reference IDs such as `reservationId` or `customerId` identify workflow records but do not replace workspace/user identity.
