# Endpoint configuration rules

- trim trailing slashes before appending integration paths
- keep public base URLs separate from authentication secrets
- preserve a verified rollback endpoint during domain cutover
- do not use endpoint configuration to alter plan entitlement or canonical ownership
- document the runtime variable required by the deployed calling context
