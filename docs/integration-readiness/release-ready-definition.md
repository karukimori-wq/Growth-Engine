# Domain release readiness

Numeria domain release is ready only when the independent domain loads, authentication works, required paths are compatible, Growth Engine CI is green, the runtime target/rollback values are known, and post-deploy verification is prepared.
