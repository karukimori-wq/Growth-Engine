# Why this approach

Keeping routine verification in CI and domain switching in runtime configuration lets Growth Engine continue development without repeatedly touching Production. It also makes the Numeria domain change reversible and keeps cross-app contracts stable while hosting/auth details evolve.
