# Quick readiness checks

- CI green?
- Numeria target domain/auth verified?
- runtime base URL known?
- reference-only handoff still enforced?
- Velvet sensitive-data exclusions still enforced?
- Business still closed?
- no schema/ownership drift?

If only the first item is ready, merge readiness work but do not deploy the domain cutover yet.
