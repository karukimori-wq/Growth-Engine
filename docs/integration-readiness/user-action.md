# When user action is required

No user action is needed for routine PR CI.

User/deployment-owner action is required only when the Numeria independent-domain release is ready, because Cloudflare runtime configuration and the Production workflow are external release operations. Those actions should be requested together, once, with exact values and verification steps.
