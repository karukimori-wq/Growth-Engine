# Production workflow rule

Use Cloudflare Production to release a prepared change, not to discover whether ordinary code compiles or contract tests pass. Those checks belong to CI and should be completed first.
