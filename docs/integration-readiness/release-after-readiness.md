# Release after readiness

Once readiness is merged, the domain release can be handled as a small configuration release rather than another broad code migration. That is the intended outcome of this preparation.
