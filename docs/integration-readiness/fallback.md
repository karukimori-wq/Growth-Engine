# Endpoint fallback intent

The legacy Numeria endpoint is retained only as a temporary safe fallback/rollback context. It is not the desired long-term production domain once `numeria-studio.com` is verified and deployed successfully.
