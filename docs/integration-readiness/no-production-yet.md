# Current deployment hold

This readiness branch does not require a Cloudflare Production run by itself.

The Numeria independent-domain cutover should be deployed only after the target domain and authentication flow are verified and the required Growth Engine runtime base URL is configured. Until then, code/documentation/contract-test work should continue through pull-request CI.
