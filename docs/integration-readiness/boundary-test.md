# Boundary test philosophy

Test the contract boundary that Growth Engine controls: required reference identity, forbidden sensitive fields, explicit data-safety flags, build/type compatibility, and stable endpoint construction. External provider uptime belongs to release verification, not deterministic contract CI.
