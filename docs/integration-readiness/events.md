# Studio event contract names

Growth Engine integration work uses the current Studio event names:

- `studio.session.started.v1`
- `studio.session.completed.v1`
- `studio.report.generated.v1`

Common integration status values are `success`, `warning`, `error`, and `skipped`. Legacy event names should not be reintroduced during endpoint or domain changes.
