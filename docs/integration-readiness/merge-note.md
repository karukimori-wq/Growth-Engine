# Merge note

Merging this readiness work is safe only after CI passes. Merge does not perform the Numeria independent-domain cutover because Production runtime configuration remains unchanged until the later release action.
