# Notes

The previous Numeria ChatGPT Site URL remains fallback/rollback context during preparation. This is intentional and avoids forcing a domain switch merely by merging code.

Once the independent domain is verified in Production, the fallback can be reconsidered in a later cleanup change rather than during the cutover itself.
