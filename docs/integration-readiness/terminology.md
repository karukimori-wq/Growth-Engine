# Integration terminology

Use `Report` for Numeria Studio report artifacts. Do not reintroduce legacy `Document` terminology for the Report contract.

Use the official Studio event names and common status values. Endpoint/domain changes are transport configuration and must not silently redefine shared contract terminology.
