# Batched release behavior

Related code, tests, docs, and runtime requirements should be prepared together. The deployment owner should receive one clear release request only after the batch is green and external prerequisites are ready.
