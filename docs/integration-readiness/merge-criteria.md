# Merge criteria

Merge this readiness branch when its PR is mergeable and CI passes all routine verification steps. No Production result is required for merge because Production cutover is intentionally deferred.
