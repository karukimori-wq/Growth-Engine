# Final readiness check

Before merge, rely on CI rather than Production. Before domain release, rely on verified external-domain/auth readiness plus the documented runtime variable, rollback, and post-deploy checks.
